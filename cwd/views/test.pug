doctype html
html
  head
    title Citywide Dashboard

    <style type="text/css">
      body,
      * {
        padding: 0;
        margin: 0;
      }

      #map {
        height: 80vh;
        width: 80vw;
      }

      svg * {
        transform-origin: 50% 50%;
      }

      .draggable {
        cursor: move;
      }
    </style>

  body
    script .
        var testthis = #{testVar};
        console.log(testthis);
    div(id="page-wrap")
        div(id="dashboard-wrap")
            div(id="map")

    script(src="./dist/cwd-bundle.js")
    script(src="./app.js")